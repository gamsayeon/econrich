<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.econrich.mapper.JobHistoryMapper">
    <!-- resultMap -->
    <resultMap id="EmployeeJobHistoryResultMap" type="com.example.econrich.dto.EmployeeJobHistoryDTO">
        <id property="employeeId" column="employee_id" />
        <result property="startDate" column="start_date" />
        <result property="endDate" column="end_date" />
        <result property="jobTitle" column="job_title" />
        <result property="departmentName" column="department_name" />
        <result property="streetAddress" column="street_address" />
        <result property="postalCode" column="postal_code" />
        <result property="city" column="city" />
        <result property="stateProvince" column="state_province" />
        <result property="countryName" column="country_name" />
        <result property="regionName" column="region_name" />
    </resultMap>

    <select id="findJobHistoryById" resultType="EmployeeJobHistoryResultMap">
        SELECT jh.employee_id, jh.start_date, jh.end_date, j.job_title, d.department_name,
        l.street_address, l.postal_code, l.city, l.state_province, c.country_name, r.region_name
        FROM job_history jh
        JOIN departments d ON d.department_id = jh.department_id
        JOIN jobs j ON j.job_id = jh.job_id
        JOIN locations l ON d.location_id = l.location_id
        JOIN countries c ON l.country_id = c.country_id
        JOIN regions r ON c.region_id = r.region_id
        WHERE jh.employee_id = #{employeeId}
    </select>
</mapper>